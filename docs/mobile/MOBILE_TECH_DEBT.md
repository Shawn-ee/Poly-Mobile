# Mobile Technical Debt

Purpose: Track accepted technical debt openly.

Priority:

- P0: Must fix before prototype acceptance.
- P1: Should fix before broader internal testing.
- P2: Can wait until after prototype.

| ID | Problem | Why Accepted For Now | Risk | Fix Plan | Priority | Introduced | Resolved |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TD-001 | `npm audit` reports 11 moderate advisories in the Expo dependency tree. | Phase 0 goal is environment/bootstrap verification; no exploit path reviewed yet. | Dependency risk may grow if ignored. | Run focused audit after app shell stabilizes; avoid breaking Expo SDK dependencies blindly. | P2 | Cycle 001 |  |
| TD-002 | Bootstrap UI is light-mode and not yet close to Holiwyn's target dark-first World Cup UX. | Existing scaffold was used only to prove emulator/runtime workflow. | Future cycles could drift if not replaced quickly. | Cycle 002 should build the Holiwyn dark-first app shell and mock World Cup home. | P1 | Cycle 001 | Cycle 002 |
| TD-003 | Repo-local app currently has no seeded/mock World Cup markets and shows empty state against current backend. | Phase 0 needed launch proof; mock data belongs in first product cycle. | QA cannot test event detail or trade ticket until mock markets exist. | Add `mobile/src/mocks/` and switch app to mock-first World Cup data in Cycle 002. | P1 | Cycle 001 | Cycle 002 |
| TD-004 | Cycle 002 UI uses mock World Cup data and local mock orders instead of backend market/order APIs. | The first product cycle needed a complete mobile interaction model before server adapter work. | App behavior can diverge from backend contracts if adapter work is delayed. | Add a typed API adapter that can normalize backend markets into the World Cup UI shape, then route mock order actions through a server-compatible service boundary. | P0 | Cycle 002 |  |
| TD-005 | Long market lists need more bottom safe-area spacing on small emulator viewports. | Cycle 002 was accepted because all rows are reachable by scrolling and no crash occurs. | Last visible rows can feel crowded near the bottom navigation. | Add a reusable screen container/list inset and verify on emulator screenshots. | P2 | Cycle 002 |  |
| TD-006 | Category icons and flags are placeholder emoji/text assets. | They are fast placeholders for product flow verification. | Visual quality and brand consistency are below final app standard. | Replace with a small Holiwyn asset set after core flows stabilize. | P2 | Cycle 002 |  |
| TD-007 | Mobile trade ticket still places local mock orders instead of authenticated backend orders. | Backend order routes require auth/trading guards and should not be rushed into mobile without a mode boundary. | App can appear tradable while not creating server-side orders. | Add a mobile order service with mock and backend modes, then verify authenticated local backend order placement before enabling server mode. | P0 | Cycle 003 |  |
| TD-008 | Mobile server order mode is implemented as a guarded code path but is not authenticated or QA-verified. | Current product requirement is fake-token trading first, and authenticated trading requires backend/user-session readiness. | Enabling server mode too early could fail silently or create inconsistent orders. | Verify auth/session flow, submit a small local backend order, and only then allow server mode in QA builds. | P0 | Cycle 004 |  |
| TD-009 | Localization copy table still lives in `mobile/App.tsx`. | Broad copy extraction touched encoding-sensitive text, so Cycle 005 extracted only ASCII-safe helpers. | Future UI edits can keep bloating the main app file. | Normalize source encoding and move copy into `mobile/src/presentation/copy.ts`. | P2 | Cycle 005 |  |
| TD-010 | Deep smoke uses fixed emulator tap coordinates. | It gives immediate overnight coverage without adding a full mobile automation stack. | Coordinates may break on different emulator dimensions or layout changes. | Add test IDs and migrate deep smoke to selector-based tooling. | P2 | Cycle 013 | Partial in Cycles 015 and 017 |
| TD-011 | Deep smoke still taps by fixed coordinates even though hierarchy assertions now verify landed screens. | Android hierarchy assertions were the safest incremental harness improvement. | Layout or emulator-size changes can still cause taps to miss before assertions run. | Move tap actions to native selectors or a mobile E2E runner once the app structure stabilizes. | P2 | Cycle 017 | Partial in Cycles 018 and 019 |
| TD-012 | Typed-query smoke disables Search soft input with a smoke-only Expo flag. | Emulator stylus handwriting intercepted ADB text before React Native received it. | Smoke behavior has a small test-only branch that real users do not exercise. | Replace coordinate/ADB text entry with selector-based mobile E2E input. | P2 | Cycle 023 |  |
| TD-013 | Live refresh is local UI state only and does not reload backend events or live odds. | Cycle 024 added the user-facing freshness affordance before wiring real refresh behavior. | Users can see a refresh action that does not yet fetch new market data. | Connect Live refresh to the mobile event adapter and backend/mock reload path. | P1 | Cycle 024 |  |
