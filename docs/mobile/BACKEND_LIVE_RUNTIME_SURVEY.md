# Backend Live Runtime Survey

Generated for the Backend Live Runtime Survey + One Event Live Pipeline goal.

## Current Truth

The current Local MVP is backend-owned and fake-token only. It has proven one sportsbook event with Odds API replay data, route visibility, order placement, Portfolio, cashout/sell, and History. That proof is not the same as a continuous live provider/runtime.

## Existing Pieces

| Area | Existing implementation | Current runtime status |
| --- | --- | --- |
| Odds API import | `src/server/services/theOddsApiSingleEventProvider.ts` and `scripts/seed_the_odds_api_single_event.ts` can fetch or replay one soccer event, normalize markets, upsert `Event`, `Market`, `Outcome`, and `ReferenceQuoteSnapshot`. | Live fetch is one-shot. Replay is default for proof. No long-lived polling loop was present before this goal. |
| One-command local onboarding | `scripts/onboard_holiwyn_one_event_live_runtime.ps1` replays or optionally live-refreshes one provider-shaped event, then runs readiness, runtime status, settlement readiness, and manual settlement dry-run. Optional `-StartRuntimeLoops -StopRuntimeLoopsAfterProof` starts the local supervisor/result-poller, proves both are running, and cleans them up. | Proven in quota-free replay mode. Live provider mode requires explicit `-RunProviderRefresh` and `THE_ODDS_API_KEY`. Runtime loop proof is explicit and still does not install an OS service. |
| Cached live-runtime restore | `scripts/restore_odds_api_one_event_from_live_summary.ts` restores the reusable local one-event slug from the latest passing live-runtime summary when the redacted replay fixture is older than the selected upcoming event. | Proven in quota-free onboarding mode. It prevents stale replay drift without spending provider quota. |
| Quota protection | `assertQuotaBudget`, `quotaCost`, redacted call records, and `MAX_CREDITS=8` in the seed script. | Present for one-shot import. Needs explicit refresh policy for live loop cadence. |
| Mobile route stack | `GET /api/events`, `GET /api/mobile/events/:slug/live-detail`, `GET /api/markets/:id/quote`, `POST /api/orders`, `GET /api/portfolio`, `GET /api/portfolio/history`. | Proven for replay event. Can support a live-refreshed backend event if snapshots/orders are seeded. |
| Provider lifecycle surface | `mobileLiveEventDetail.ts` reports quote/depth/chart lifecycle as `ready`, `refresh_due`, `stale`, or `unavailable`. Quote snapshots are stale after 90 seconds and refresh-due after 60 seconds. | Exists. Needs a provider refresh runner to keep snapshots fresh. |
| Durable provider refresh records | `ProviderRefreshRun`, `scripts/write_provider_refresh_run.ts`, `npm run mobile:provider-refresh-run-record`, and `scripts/prove_odds_api_one_event_live_runtime.ts` persist bounded live-provider refresh proof outcomes. | Proven without a new provider call by backfilling from the current redacted live-provider proof. Future explicit live-provider proofs write this row automatically. The status route and audits require the latest selected-event row to be passed, quota-protected, within budget, stale-before-refresh, and ready-after-refresh. |
| Provider refresh loop status | `GET /api/internal/live-runtime/status` exposes `providerRefreshLoop`, combining current supervisor provider-refresh configuration, cadence, quota caps, latest durable `ProviderRefreshRun`, and mobile freshness thresholds. | Proven read-only without provider quota. It makes the default cached/no-quota mode, explicit `RunProviderProof` requirement, `THE_ODDS_API_KEY` requirement, 60-second refresh-due threshold, 90-second stale threshold, and latest live-refresh budget truth machine-readable. |
| Durable maker quote records | `MarketMakerQuoteRun`, `scripts/write_market_maker_quote_run.ts`, `npm run mobile:market-maker-quote-run-record`, and `scripts/seed_odds_api_live_shifted_maker.ts` persist shifted local maker quote proof outcomes for the selected market/outcome. | Proven without provider quota by backfilling from the current shifted maker proof and by running the real maker seed path twice. The status route and audits require the latest selected-market row to be passed, local-only, shifted worse than provider, quote-route visible, backed by a fresh provider snapshot, and supported by at least two recent seed-script-emitted rows. |
| Provider-to-maker handoff report | `scripts/report_holiwyn_provider_maker_handoff.ts` and `npm run mobile:provider-maker-handoff` verify durable handoff from a passed selected-event provider refresh to later shifted maker quotes for the same event, market, and outcome. | Proven without provider quota. Phase and completion audits now require this no-quota handoff evidence, narrowing automatic quote replacement while still preserving the P1 truth that no installed production maker daemon exists. |
| Local matching/order flow | `matching.ts`, canonical order route, Portfolio/history routes. | Proven for fake-token buy/sell and negative sell cases. |
| One-shot maker liquidity | Internal harnesses mint complete sets and place maker ask/bid orders directly with `placeOrderAndMatch`. | Works for proof. Not continuous by default. |
| One-event local supervisor | `scripts/run_holiwyn_one_event_live_supervisor.ps1` repeats data hygiene, the proven runtime launch command, local maker reseeding, and the safe real-time lifecycle scheduler on an interval. It can optionally run quota-guarded provider refresh, stale guard, replay result ingestion, opt-in quota-capped live result ingestion, and trusted-result settlement scheduling, and it writes heartbeat evidence after each cycle. | Continuous only while the local command is running. `npm run mobile:one-event-live-supervisor:continuous-proof` proves repeated local cycles, replay-mode result ingestion, dry-run result settlement scheduling, and clean stop behavior. Live result ingestion requires `-RunLiveResultIngestion` plus `THE_ODDS_API_KEY` and is capped by run count/cadence/credits. It is not an installed unattended service. |
| Internal tester runtime manager | `scripts/manage_holiwyn_internal_tester_runtime.ps1` provides local start/status/stop orchestration for backend, Expo, Docker/Postgres visibility, S23 reachability, one-event supervisor status, and dedicated result-poller status/control. It reuses existing backend/Expo listeners when ports are already occupied and stops only manager-owned backend/Expo processes, while delegating supervisor/result-poller shutdown to their own process managers. Manager-owned Expo records verified server-mode env; reused external Expo is explicitly reported as `external_listener_unverified` so stale/non-server phone behavior is not mistaken for a proven tester runtime. Operator aliases now expose the intended modes: `npm run mobile:internal-tester-runtime:cached-start`, `npm run mobile:internal-tester-runtime:live-provider-start`, and `npm run mobile:internal-tester-runtime:stop`. | Status proof and result-poller control proof passed without provider quota. Cached start remains the default internal tester mode. Live-provider start is explicit, requires `THE_ODDS_API_KEY`, and routes through the existing quota-capped provider proof path. This is an internal local control plane for tester launch, not an installed OS service or production daemon. |
| Worker-owned runtime heartbeats | `scripts/write_runtime_service_heartbeat.ts` and `npm run mobile:runtime-heartbeat` let the one-event supervisor and dedicated result poller emit `RuntimeServiceHeartbeat` rows while they run. `GET /api/internal/live-runtime/status` preserves that worker-owned metadata while also mirroring current process-state checks. | Proven without provider quota. The current rows preserve `workerOwned=true` for both local services, record no installed OS service, and record no quota-spending loop. This is stronger durable local status evidence, but it is still local-process evidence rather than production service ownership. |
| Worker-owned runtime run records | `scripts/write_runtime_service_run.ts`, `npm run mobile:runtime-run-record`, `RuntimeServiceRun`, and the supervisor/result-poller loop scripts persist a finished-run row after each local worker run. `GET /api/internal/live-runtime/status`, the phase audit, and the completion audit now require latest passed rows for both services. | Proven without provider quota. The current rows record service kind, status, start/finish, iteration count, event/market identity when available, no provider quota spend, no active settlement execution, and no installed OS service. This narrows proof-artifact-only runtime evidence but is still not production daemon ownership. |
| Local runtime scheduled-task manager | `scripts/manage_holiwyn_local_runtime_task.ps1` plans, reports, installs, or uninstalls a Windows scheduled task that starts the internal tester runtime. `scripts/prove_holiwyn_local_runtime_task_install_uninstall.ps1` audits install/remove safety. | Dry-run plan proof passed without provider quota and confirmed no scheduled task is installed by default. Install/uninstall audit confirms Windows denies registration in the current process context, cleanup leaves no task installed, and elevated/task-registration rights are needed before applying. |
| Local runtime user Startup launcher | `scripts/manage_holiwyn_local_runtime_startup.ps1` plans, reports, installs, or uninstalls a current-user Windows Startup folder `.cmd` launcher that starts the internal tester runtime at user logon. It can carry result ingestion, trusted-result settlement scheduling, approved-settlement wait mode, and the dedicated result poller through the runtime manager parameter chain. `scripts/prove_holiwyn_local_runtime_startup_install_uninstall.ps1` audits install/remove safety with a proof-only launcher. | Plan and install/uninstall proof are local-only and spend no provider quota by default. Current proof includes the approved-settlement supervisor profile plus `-StartResultPoller` and leaves no persistent launcher installed. This is a practical user-logon fallback when scheduled-task registration is denied, not an installed production service or health-monitored daemon. |
| Local runtime launch profile | `scripts/report_holiwyn_local_runtime_launch_profile.ts` reads the runtime status, internal tester manager, supervisor/result-poller, Startup launcher, and scheduled-task proof artifacts. | Proven read-only without provider quota. It recommends the user Startup fallback in the current Windows context, records the scheduled-task permission blocker, lists manual cached-start/stop commands, exposes `npm run mobile:internal-tester-runtime:live-provider-start` as the explicit live-provider profile, and documents `npm run mobile:internal-tester-runtime -- -Action start -Force -ReplaceExternalExpo -WaitForReady` for replacing stale Expo with verified server-mode Expo. Live-provider mode remains opt-in and quota/key gated. |
| Live-runtime completion audit | `scripts/report_holiwyn_live_runtime_completion_audit.ts` reads runtime status, live provider proof, readiness, launch profile, internal tester watchdog, stale guard, lifecycle, active-settlement readiness, active-event closed-state eligibility, and S23 proof artifacts. | Proven read-only without provider quota. It directly answers the phase completion questions for maker continuity, live/replay odds mode, refresh cadence, quota protection, stale handling, lifecycle, local watchdog health, active closed-state settlement eligibility, and mobile trade proof while keeping unattended service and production official-result auto-settlement as P1. |
| Machine-readable completion requirements | `scripts/report_holiwyn_live_runtime_completion_audit.ts` writes `completionRequirements`, and `GET /api/internal/live-runtime/status` projects `phaseCompletion.completionRequirements`. | Proven read-only without provider quota. The status route and phase audit now require pass/fail rows for market-maker continuity, odds refresh mode, quota protection, stale odds handling, event lifecycle, one-event mobile trading, and runtime launch, so the local phase completion questions are machine-checkable instead of only prose. |
| Runtime service ownership summary | `GET /api/internal/live-runtime/status` now exposes `serviceOwnership` as the single operator-facing answer for foreground process proof vs installed OS service ownership. Phase and completion audits require it. | Proven read-only without provider quota. It reports that the local MVP uses foreground supervisor/result-poller worker processes, that market making is continuous only while the supervisor runs, that live-provider refresh is explicit and quota/key gated, and that no installed production service is present. |
| Current warm no-quota runtime proof | `scripts/prove_holiwyn_current_runtime_state.js` powers `npm run mobile:current-runtime-state-proof`. It starts the local internal tester manager, observes `/api/internal/live-runtime/status` while supervisor and result-poller loops are actually running, then stops manager-owned processes. | Proven without provider quota. Latest proof reports `warm_no_quota_runtime`, both loops running, no quota-spending loop, and clean loop shutdown. This proves current local warmth separately from older capability artifacts while keeping fresh mobile-route provider odds as P1 when no live refresh is run. |
| Internal tester watchdog | `scripts/run_holiwyn_internal_tester_watchdog.ps1` checks the local runtime manager status, starts backend/Expo only if the base runtime is missing, runs the repeated supervisor proof, runs the background result-poller proof, and optionally stops loop processes after the proof. | Proven with `npm run mobile:internal-tester-watchdog-proof` without provider quota. It confirms backend/Expo/Postgres readiness, supervisor loop capability, background result-poller capability, and clean loop cleanup. It is a local proof watchdog, not an installed OS service. |
| Continuous bot/soak harness | `scripts/soak_orderbook_bots.ts` references a sibling `poly-bot` package. `scripts/create_sim_bot_credentials.ts` writes bot config to `../poly-bot`. | Not self-contained in this repo. Do not assume it is running or available for mobile MVP runtime. |
| Reference liquidity seeding | `referenceLiquiditySeeding.ts` supports approved Polymarket reference markets only. | Not usable for Odds API sportsbook markets without new source-aware logic. |
| Event pause/close/resolve | Admin routes can pause/close markets and resolve orderbook markets. | Manual/admin lifecycle exists. No automatic soccer settlement or result ingest exists yet. |
| One-event lifecycle scheduler | `src/server/services/oneEventLifecycleScheduler.ts` can pause the selected event inside the pre-start suspend window and close it at/after start. `scripts/run_odds_api_one_event_lifecycle_scheduler.ts` runs it safely against the real current time; `scripts/prove_odds_api_event_lifecycle_scheduler.ts` proves pause/close by temporarily mutating start times and restoring them. | Proven locally. The supervisor now calls the safe scheduler each cycle while active, but it is not installed as an unattended daemon/service. |
| One-event lifecycle matrix | `scripts/report_odds_api_one_event_lifecycle_matrix.ts` reads lifecycle-control, lifecycle-scheduler, settlement, trusted-result scheduler, and result-review proof artifacts plus current event status. | Proven read-only without provider quota. It summarizes open, paused, closed, and settled/resolved evidence in one matrix and explicitly states that active tester settlement has not executed. |
| Local lifecycle status API | `GET /api/internal/live-runtime/lifecycle` reads the same lifecycle/settlement proof evidence plus the current DB event and listed sportsbook markets. | Proven read-only without provider quota. It exposes open, suspended, closed, and settled/resolved proof truth to internal tools, confirms active tester settlement has not executed, redacts confirmation state, and is gated by the phase audit. It is not a mutation or settlement execution endpoint. |
| One-event runtime status capability report | `scripts/report_odds_api_one_event_runtime_status.ts` reads local health/quote routes plus latest and continuous runtime proof summaries. | Proven read-only without provider quota. It separates the latest supervisor run profile from broader proven supervisor/result-poller capabilities so operator status does not under-report capabilities after a narrow proof run. |
| Stale provider trading guard | `scripts/prove_odds_api_one_event_stale_guard.ts` proves local stale-data handling by forcing selected provider snapshots stale, pausing the market, verifying `POST /api/orders` rejects, and restoring the market/snapshots. | Proven as a local callable proof. It is not installed as an unattended service. |
| Supervisor stale-provider monitor/enforcer | `scripts/run_odds_api_one_event_stale_guard.ts` can run in dry-run monitor mode or enforcement mode, and `run_holiwyn_one_event_live_supervisor.ps1` can invoke it every cycle with `-RunStaleGuard`. | Proven in one supervisor cycle in dry-run mode. Enforcement is available through `-EnforceStaleGuard`, but it is still local process behavior. |
| One-event phase audit | `scripts/report_odds_api_live_runtime_phase_audit.ts` reads all live-runtime proof summaries plus local health/quote routes and reports P0/P1/P2 phase status. | Proven locally. Current result has 0 open P0 gaps for the local internal one-event runtime, with unattended services and official result auto-settlement open as P1. |
| Settlement | `settlement.ts`, `previewOrderbookSettlement`, `resolveOrderbookMarket`, admin preview/resolve routes, `scripts/settle_odds_api_one_event_market.ts`, and `scripts/prove_odds_api_one_event_settlement_execution.ts` exist for orderbook markets. | Manual/admin-driven for the active tester event. Non-mutating readiness, guarded one-event manual dry run, trusted-result dry run, and disposable local execution proof exist; unattended official result polling and unconfirmed active-event execution remain P1. |
| Trusted result settlement intake | `scripts/ingest_odds_api_one_event_result.ts` converts an Odds API scores payload into trusted soccer result JSON and can write canonical `provider.result.ingested` audit evidence. `scripts/settle_odds_api_one_event_from_result.ts` maps that result evidence to a winning outcome, previews settlement, and keeps execution behind an exact result-digest confirmation phrase plus a closed-market execution guard. `scripts/run_odds_api_one_event_result_settlement_scheduler.ts` can run that path from a local scheduler/cron style command, dry-run by default. `scripts/report_odds_api_one_event_settlement_preflight.ts` reports active-event execution eligibility and blockers without mutation. `scripts/prove_odds_api_trusted_result_settlement_scheduler_execution.ts` proves the scheduler execute path on a disposable sportsbook-shaped event. | Provider-shaped replay ingestion, durable result-ingestion audit evidence, and dry-run scheduling are proven for Spain vs. France Total Goals 2.5. Active-event preflight reports final trusted result evidence and passing payout preview, but current execution is blocked by `market_not_closed_for_execution:LIVE`. Trusted-result scheduler execution is proven on disposable local evidence without mutating the active tester event, including a live-market blocked execution check before closing and settling the disposable market. Live score ingestion is available through explicit `--live` or supervisor `-RunLiveResultIngestion`, both requiring `THE_ODDS_API_KEY`; installed unattended official result polling and unconfirmed active-event execution remain P1. |
| Dedicated local result poller | `scripts/run_holiwyn_one_event_result_poller.ps1` repeatedly runs provider-shaped score ingestion and trusted-result settlement scheduling, with replay/no-quota default mode and explicit quota-capped live score polling via `-RunLiveResultIngestion`. `scripts/manage_holiwyn_one_event_result_poller.ps1` wraps it with local start/status/stop process management, and `scripts/prove_holiwyn_one_event_continuous_result_poller.ps1` proves background start, heartbeat progress, and clean stop. | Proven locally for two cycles without provider quota, and as a hidden local background process without provider quota. It produces its own summary and heartbeat, regenerates trusted result JSON each cycle, dry-runs settlement scheduling each cycle, and does not execute active tester settlement. It is a local managed process, not an installed unattended official-result service. |
| Trusted result settlement audit trail | `settle_odds_api_one_event_from_result.ts --writeAuditEvent` writes a canonical market event through `emitMarketSettlementAuditEvent`. `scripts/prove_odds_api_settlement_audit_event.ts` proves the dry-run path creates a durable `settlement.trusted_result.preflight` event for the selected market/outcome/result digest. | Proven without provider quota and without settling the active tester event. This closes durable local audit evidence for reviewed settlement preflight, but not unattended official result polling or automatic execution. |
| Trusted result approval audit trail | `scripts/prove_odds_api_settlement_approval_audit_event.ts` derives approval fields from the active-event trusted-result dry run, writes a canonical `settlement.trusted_result.approved` market event, and exports a matching local approval file for scheduler compatibility. | Proven without provider quota and without settling the active tester event. This narrows durable approval evidence, but a first-class approval table/operator UI and installed official-result polling remain P1/P2. |
| One-event result review trail | `scripts/report_odds_api_one_event_result_review_trail.ts` reads canonical `provider.result.ingested`, `settlement.trusted_result.preflight`, and `settlement.trusted_result.approved` events for the active event and selected market. | Proven read-only without provider quota. It gives an operator one local report that connects provider result evidence, settlement preflight evidence, approval evidence, selected market/outcome, current market status, digest matching, and the next execution decision. It is not a dedicated official-result table or review UI. |
| Local result-review API | `GET /api/internal/live-runtime/result-review` reads the selected one-event market plus canonical `provider.result.ingested`, `settlement.trusted_result.preflight`, `settlement.trusted_result.approved`, and `settlement.trusted_result.executed` events. | Proven read-only without provider quota and gated by the phase audit. It gives local internal tooling a backend route for the canonical result/settlement trail and redacts exact confirmation strings. It is not a public mobile route, execution endpoint, dedicated official-result table, or operator UI. |
| Local settlement queue API | `GET /api/internal/live-runtime/settlement-queue` reads durable `OfficialResultReview` rows plus current market/event status and returns redacted queue items with safe next actions and a redacted `operatorAction` plan. | Proven read-only without provider quota and gated by the phase audit. It gives local internal tooling a backend route for pending/approved settlement work, including whether to wait for close, rerun preflight, record approval, or use the guarded approved scheduler command. It still does not execute settlement or expose exact confirmation strings. It is not a public mobile route or authenticated production operator UI. |
| Durable settlement approval evidence | `GET /api/internal/live-runtime/settlement-queue` and `GET /api/internal/live-runtime/status` expose redacted approval evidence derived from `OfficialResultReview` and canonical approval event IDs. | Proven read-only without provider quota. Approved queue items now explicitly report whether approval is backed by a durable review row and canonical approval event, and phase/completion audits fail if approved reviews lack that evidence. This narrows the first-class approval evidence gap while still leaving authenticated operator UI and production execution controls as P1/P2. |
| Durable settlement execution evidence | `GET /api/internal/live-runtime/settlement-queue` and `GET /api/internal/live-runtime/status` expose redacted execution evidence derived from `OfficialResultReview.settlementExecutedCanonicalId` and canonical execution events. | Proven by focused tests without provider quota. Executed queue items can now explicitly report whether execution is backed by a durable review row and canonical execution event, while unexecuted active tester rows still report `not_executed` without exposing exact confirmation strings. This narrows the official-result auto-settlement evidence gap while leaving authenticated operator UI and production execution controls as P1/P2. |
| Result-review completion/status gate | `scripts/report_holiwyn_live_runtime_completion_audit.ts` consumes phase-audit `localResultReview`, and `GET /api/internal/live-runtime/status` exposes `resultReview` readiness. | Proven read-only without provider quota. Completion/status truth now fails or reports attention if canonical result-review evidence is missing, not ready, quota-spending, not read-only, or has P0 gaps. |
| Settlement queue completion/status gate | `scripts/report_odds_api_live_runtime_phase_audit.ts` consumes `/api/internal/live-runtime/settlement-queue`, and `GET /api/internal/live-runtime/status` exposes `settlementQueue` readiness plus the redacted first-item operator plan. | Proven read-only without provider quota. Runtime status now needs attention if the settlement queue route is missing, not ready, quota-spending, execution-attempting, exposes exact confirmation strings, lacks a redacted operator plan, or has P0 gaps. |
| Durable official-result review record | `OfficialResultReview` mirrors the local result-review route's provider-result, settlement-preflight, and settlement-approval evidence into first-class backend storage. | Proven locally without provider quota. The record is redacted, stores no exact settlement confirmation phrase, does not mutate markets, and is now required by phase/completion/status gates. It narrows durable result/approval storage but is still not an operator UI, multi-event queue, or production execution service. |
| Active-event settlement execution decision | `scripts/report_odds_api_active_event_settlement_readiness.ts` reads the active market, settlement preflight, approval audit event, result review trail, supervisor approved-settlement wait proof, and active-event clone proof. | Proven read-only without provider quota. It reports that the active selected market has approval/preflight evidence but is not execution-ready while status remains `LIVE`; operator decision is to wait for/apply market close before exact confirmed execution. |
| Approved trusted-result auto-settlement | `run_odds_api_one_event_result_settlement_scheduler.ts --autoExecuteApproved --approval=<path>` dry-runs first, matches the generated confirmation phrase/result digest/market/outcome/event against a local approval file, waits while the market is not `CLOSED`, and executes only after close. `scripts/prove_odds_api_approved_auto_settlement.ts` proves the path on a disposable sportsbook-shaped event. | Proven without provider quota and without mutating the active tester event. This narrows automatic settlement to operator-approved local automation; installed unattended official result polling and unconfirmed active-event execution remain P1. |
| Active-event closed-state settlement eligibility | `scripts/prove_odds_api_active_event_closed_settlement_eligibility.ts` temporarily closes the active selected market, runs the guarded trusted-result dry run with audit-event output, and restores the original market state in `finally`. | Proven without provider quota and without executing active-event settlement. It shows the active selected market would be eligible for exact-confirmation execution after close, while preserving the current `LIVE`/unresolved tester market. |
| Supervisor approved settlement wait mode | `run_holiwyn_one_event_live_supervisor.ps1 -RunResultSettlement -RunApprovedResultSettlement -ResultSettlementApprovalPath <path>` passes trusted result JSON, approval matching, and audit-event flags into the scheduler while preserving the closed-market guard. `scripts/prove_holiwyn_supervisor_approved_settlement_wait.ps1` proves the active tester market waits while `LIVE`. | Proven without provider quota and without mutating the active tester event. This wires approval-file settlement into the local supervisor, but installed unattended official result polling remains P1. |

## Odds API Usage Classification

| Mode | Current support |
| --- | --- |
| Replay fixture only | Yes. `npm run mobile:odds-api-internal-env-proof` defaults to committed redacted Odds API evidence and makes no provider calls. |
| One-time live import | Yes, when `THE_ODDS_API_KEY` is provided to `npm run mobile:the-odds-api-single-event`. |
| Live provider polling | Not previously present as a product runtime. Added as a minimal proof script in this goal, not yet a production daemon. |

## Missing For A Real Live Local Event

| Gap | Priority | Notes |
| --- | --- | --- |
| Source-aware Odds API refresh loop | P0 for this goal | Need configurable interval, quota cap, stale detection, and failure handling. |
| Continuous local market maker tied to provider odds | P0 for this goal | Need maker quotes shifted worse than provider, with risk caps and cleanup. |
| Auto-close/suspend near event start or provider unavailable | P1 | Start-time pause/close now runs inside the foreground supervisor while it is active. A stale-provider pause guard is proven as a callable command. Neither is installed as an always-on daemon. |
| Automatic settlement from official result | P1 | Existing settlement can ingest provider-shaped score evidence into trusted result JSON and run a scheduler-style dry run, but live score ingestion is explicit/manual and no unattended result polling or unconfirmed execution is wired. |
| Production bot daemon ownership | P1 | `poly-bot` is not inside this repo, so mobile repo must not claim continuous bot readiness from that script alone. |

## Survey Conclusion

The backend is close enough for a one-event local live proof because the data model, mobile routes, fake-token matcher, provider snapshot model, and manual lifecycle routes already exist. The missing piece is a small live runner that refreshes one provider event, keeps reference snapshots fresh, places local maker quotes based on those snapshots, and proves mobile route/order/portfolio behavior against the same event.

## Proof Added In This Goal

- Command: `npm run mobile:odds-api-live-runtime-proof`.
- Restart/runtime command: `npm run mobile:one-event-live-runtime`.
- Explicit live-provider wrapper: `npm run mobile:one-event-live-runtime:provider`.
- Script: `scripts/prove_odds_api_one_event_live_runtime.ts`.
- Runtime launcher: `scripts/start_holiwyn_one_event_live_runtime.ps1`.
- Reusable maker seed: `scripts/seed_odds_api_live_shifted_maker.ts`.
- Lifecycle controls proof: `scripts/prove_odds_api_event_lifecycle_controls.ts`.
- Lifecycle scheduler proof: `scripts/prove_odds_api_event_lifecycle_scheduler.ts`.
- Safe lifecycle scheduler runner: `scripts/run_odds_api_one_event_lifecycle_scheduler.ts`.
- Consolidated readiness gate: `scripts/prove_holiwyn_one_event_live_readiness.ps1`.
- One-event local supervisor: `scripts/run_holiwyn_one_event_live_supervisor.ps1`.
- Continuous local supervisor proof: `scripts/prove_holiwyn_one_event_continuous_supervisor.ps1`.
- Provider-to-maker handoff report: `scripts/report_holiwyn_provider_maker_handoff.ts`.
- Settlement readiness report: `scripts/report_odds_api_one_event_settlement_readiness.ts`.
- Guarded manual settlement command: `scripts/settle_odds_api_one_event_market.ts`.
- Disposable settlement execution proof: `scripts/prove_odds_api_one_event_settlement_execution.ts`.
- Provider-shaped result ingestion command: `scripts/ingest_odds_api_one_event_result.ts`.
- Provider-shaped result ingestion audit proof: `scripts/prove_odds_api_result_ingestion_audit_event.ts`.
- Trusted result settlement command: `scripts/settle_odds_api_one_event_from_result.ts`.
- Trusted result settlement scheduler run: `scripts/run_odds_api_one_event_result_settlement_scheduler.ts`.
- Active event settlement preflight: `scripts/report_odds_api_one_event_settlement_preflight.ts`.
- Trusted result settlement audit event proof: `scripts/prove_odds_api_settlement_audit_event.ts`.
- Active event settlement readiness report: `scripts/report_odds_api_active_event_settlement_readiness.ts`.
- Active event closed-state settlement eligibility proof: `scripts/prove_odds_api_active_event_closed_settlement_eligibility.ts`.
- Local result-review API: `src/app/api/internal/live-runtime/result-review/route.ts` and `src/server/services/liveRuntimeResultReview.ts`.
- Local settlement queue API: `src/app/api/internal/live-runtime/settlement-queue/route.ts` and `src/server/services/liveRuntimeSettlementQueue.ts`.
- Approved trusted-result auto-settlement proof: `scripts/prove_odds_api_approved_auto_settlement.ts`.
- Supervisor approved-settlement wait proof: `scripts/prove_holiwyn_supervisor_approved_settlement_wait.ps1`.
- Dedicated result poller: `scripts/run_holiwyn_one_event_result_poller.ps1`.
- Dedicated result poller process manager: `scripts/manage_holiwyn_one_event_result_poller.ps1`.
- Dedicated result poller background proof: `scripts/prove_holiwyn_one_event_continuous_result_poller.ps1`.
- Trusted result settlement scheduler execution proof: `scripts/prove_odds_api_trusted_result_settlement_scheduler_execution.ts`.
- One-command onboarding wrapper: `scripts/onboard_holiwyn_one_event_live_runtime.ps1`.
- Cached live-runtime restore: `scripts/restore_odds_api_one_event_from_live_summary.ts`.
- Stale provider trading guard: `scripts/prove_odds_api_one_event_stale_guard.ts`.
- Supervisor stale-provider monitor/enforcer: `scripts/run_odds_api_one_event_stale_guard.ts`.
- Live-runtime phase audit: `scripts/report_odds_api_live_runtime_phase_audit.ts`.
- Internal tester runtime manager: `scripts/manage_holiwyn_internal_tester_runtime.ps1`.
- Internal tester result-poller control proof: `scripts/prove_holiwyn_internal_tester_result_poller_control.ps1`.
- Local runtime scheduled-task manager: `scripts/manage_holiwyn_local_runtime_task.ps1`.
- Local runtime scheduled-task install/uninstall proof: `scripts/prove_holiwyn_local_runtime_task_install_uninstall.ps1`.
- Local runtime user Startup launcher manager: `scripts/manage_holiwyn_local_runtime_startup.ps1`.
- Local runtime user Startup launcher install/uninstall proof: `scripts/prove_holiwyn_local_runtime_startup_install_uninstall.ps1`.
- Local runtime launch profile: `scripts/report_holiwyn_local_runtime_launch_profile.ts`.
- Live-runtime completion audit: `scripts/report_holiwyn_live_runtime_completion_audit.ts`.
- Internal tester watchdog: `scripts/run_holiwyn_internal_tester_watchdog.ps1`.
- Summary: `docs/mobile/harness/odds-api-live-runtime/one-event-live-runtime-summary.redacted.json`.
- Runtime launch summary: `docs/mobile/harness/odds-api-live-runtime/one-event-runtime-launch-summary.redacted.json`.
- Maker seed summary: `docs/mobile/harness/odds-api-live-runtime/shifted-maker-seed-summary.redacted.json`.
- Lifecycle controls summary: `docs/mobile/harness/odds-api-live-runtime/event-lifecycle-controls-summary.redacted.json`.
- Lifecycle scheduler summary: `docs/mobile/harness/odds-api-live-runtime/event-lifecycle-scheduler-summary.redacted.json`.
- Safe lifecycle scheduler run summary: `docs/mobile/harness/odds-api-live-runtime/one-event-lifecycle-scheduler-run-summary.redacted.json`.
- Lifecycle matrix summary: `docs/mobile/harness/odds-api-live-runtime/one-event-lifecycle-matrix-summary.redacted.json`.
- Local lifecycle status API: `src/app/api/internal/live-runtime/lifecycle/route.ts` and `src/server/services/liveRuntimeLifecycle.ts`.
- Settlement readiness summary: `docs/mobile/harness/odds-api-live-runtime/one-event-settlement-readiness-summary.redacted.json`.
- Settlement execution proof summary: `docs/mobile/harness/odds-api-live-runtime/one-event-settlement-execution-summary.redacted.json`.
- Manual settlement dry-run summary: `docs/mobile/harness/odds-api-live-runtime/one-event-manual-settlement-summary.redacted.json`.
- Provider-shaped score fixture: `docs/mobile/harness/odds-api-live-runtime/odds-api-score-fixture.redacted.json`.
- Provider-shaped result ingestion summary: `docs/mobile/harness/odds-api-live-runtime/one-event-result-ingestion-summary.redacted.json`.
- Provider-shaped result ingestion audit event summary: `docs/mobile/harness/odds-api-live-runtime/one-event-result-ingestion-audit-event-summary.redacted.json`.
- Result poller summary: `docs/mobile/harness/odds-api-live-runtime/one-event-result-poller-summary.redacted.json`.
- Result poller heartbeat: `docs/mobile/harness/odds-api-live-runtime/one-event-result-poller-heartbeat.redacted.json`.
- Result poller process summary: `docs/mobile/harness/odds-api-live-runtime/one-event-result-poller-process-summary.redacted.json`.
- Continuous result poller proof summary: `docs/mobile/harness/odds-api-live-runtime/one-event-continuous-result-poller-proof-summary.redacted.json`.
- Provider-ingested trusted result output: `docs/mobile/harness/odds-api-live-runtime/trusted-result-provider.redacted.json`.
- Trusted result settlement summary: `docs/mobile/harness/odds-api-live-runtime/one-event-result-settlement-summary.redacted.json`.
- Trusted result settlement scheduler summary: `docs/mobile/harness/odds-api-live-runtime/one-event-result-settlement-run-summary.redacted.json`.
- Trusted result settlement preflight summary: `docs/mobile/harness/odds-api-live-runtime/one-event-settlement-preflight-summary.redacted.json`.
- Trusted result settlement audit event summary: `docs/mobile/harness/odds-api-live-runtime/one-event-settlement-audit-event-summary.redacted.json`.
- Result review trail summary: `docs/mobile/harness/odds-api-live-runtime/one-event-result-review-trail-summary.redacted.json`.
- Active event settlement readiness summary: `docs/mobile/harness/odds-api-live-runtime/one-event-active-settlement-readiness-summary.redacted.json`.
- Active event closed-state settlement eligibility summary: `docs/mobile/harness/odds-api-live-runtime/one-event-active-settlement-closed-eligibility-summary.redacted.json`.
- Active event closed-state settlement eligibility dry run: `docs/mobile/harness/odds-api-live-runtime/one-event-active-settlement-closed-eligibility-dry-run.redacted.json`.
- Approved trusted-result auto-settlement summary: `docs/mobile/harness/odds-api-live-runtime/one-event-approved-auto-settlement-summary.redacted.json`.
- Supervisor approved-settlement wait summary: `docs/mobile/harness/odds-api-live-runtime/one-event-supervisor-approved-settlement-wait-summary.redacted.json`.
- Trusted result settlement scheduler execution summary: `docs/mobile/harness/odds-api-live-runtime/one-event-result-settlement-scheduler-execution-summary.redacted.json`.
- One-command onboarding summary: `docs/mobile/harness/odds-api-live-runtime/one-event-onboarding-summary.redacted.json`.
- Cached live restore summary: `docs/mobile/harness/odds-api-live-runtime/one-event-cached-restore-summary.redacted.json`.
- Stale guard summary: `docs/mobile/harness/odds-api-live-runtime/one-event-stale-guard-summary.redacted.json`.
- Stale guard run summary: `docs/mobile/harness/odds-api-live-runtime/one-event-stale-guard-run-summary.redacted.json`.
- Live-runtime phase audit summary: `docs/mobile/harness/odds-api-live-runtime/live-runtime-phase-audit-summary.redacted.json`.
- Local result-review API proof is embedded in the phase audit summary under `localResultReview`.
- Internal tester runtime manager summary: `docs/mobile/harness/odds-api-live-runtime/internal-tester-runtime-manager-summary.redacted.json`.
- Internal tester result-poller control summary: `docs/mobile/harness/odds-api-live-runtime/internal-tester-result-poller-control-summary.redacted.json`.
- Local runtime scheduled-task summary: `docs/mobile/harness/odds-api-live-runtime/local-runtime-task-summary.redacted.json`.
- Local runtime scheduled-task install/uninstall summary: `docs/mobile/harness/odds-api-live-runtime/local-runtime-task-install-uninstall-summary.redacted.json`.
- Local runtime user Startup launcher summary: `docs/mobile/harness/odds-api-live-runtime/local-runtime-startup-summary.redacted.json`.
- Local runtime user Startup launcher install/uninstall summary: `docs/mobile/harness/odds-api-live-runtime/local-runtime-startup-install-uninstall-summary.redacted.json`.
- Local runtime launch profile summary: `docs/mobile/harness/odds-api-live-runtime/local-runtime-launch-profile-summary.redacted.json`.
- Live-runtime completion audit summary: `docs/mobile/harness/odds-api-live-runtime/live-runtime-completion-audit-summary.redacted.json`.
- Current warm runtime proof summary: `docs/mobile/harness/odds-api-live-runtime/current-runtime-state-proof-summary.redacted.json`.
- Internal tester watchdog summary: `docs/mobile/harness/odds-api-live-runtime/internal-tester-watchdog-summary.redacted.json`.
- Consolidated readiness summary: `docs/mobile/harness/odds-api-live-runtime/one-event-live-readiness-summary.redacted.json`.
- Supervisor summary: `docs/mobile/harness/odds-api-live-runtime/one-event-live-supervisor-summary.redacted.json`.
- Supervisor heartbeat: `docs/mobile/harness/odds-api-live-runtime/one-event-live-supervisor-heartbeat.redacted.json`.
- Continuous supervisor proof summary: `docs/mobile/harness/odds-api-live-runtime/one-event-continuous-supervisor-proof-summary.redacted.json`.
- Provider-to-maker handoff summary: `docs/mobile/harness/odds-api-live-runtime/provider-maker-handoff-summary.redacted.json`.
- S23 summary: `docs/mobile/harness/cycle-ZD-spain-france-cashout-fresh/cycle-ZD-SPAIN-FRANCE-CASHOUT-FRESH-odds-api-s23-visible-flow.json`.
- Result: pass.
- The proof is bounded and local-only. It proves the minimum live path for one upcoming provider event and now has a foreground supervisor that repeats hygiene, cached runtime checks, shifted maker seeding, safe lifecycle scheduling, provider-shaped result ingestion, trusted-result settlement dry-runs, and approval-file settlement wait checks while it runs. It also has canonical result-ingestion, settlement-preflight, and settlement-approval audit evidence. A dedicated result poller repeats result ingestion plus settlement scheduling with no provider quota by default, can be managed as a hidden local background process, and is now included in both the current-user Startup launcher fallback and the Windows scheduled-task plan profile with approved-settlement wait mode. Settlement execution is proven separately on fresh disposable local markets, and active-event closed-state eligibility is proven by temporarily closing/restoring the selected active market without execution, so the active tester event is not left mutated. The current Windows process still cannot register the scheduled task, so this remains a local tester runtime rather than an unattended production daemon.
- Local live-runtime status also persists durable `ProviderRefreshRun`, `RuntimeServiceHeartbeat`, and `RuntimeServiceRun` rows so internal status survives beyond ephemeral proof files. Provider refresh rows record the bounded live-provider proof outcome and quota budget truth; local loops emit worker-owned heartbeat/run metadata; the status route preserves that evidence while recording that no quota-spending loop and no installed OS service are currently running.
- Local live-runtime status also persists durable `MarketMakerQuoteRun` rows for shifted local maker quote cycles. It now reports a recent-run window and requires at least two local seed-script-emitted rows, proving repeated local quote refresh behavior for the selected market while still recording that no installed market-maker service exists.
- Local handoff evidence now verifies the latest selected-event provider refresh has a later shifted maker quote run for the same event, market, and outcome. This narrows automatic quote replacement evidence without claiming an installed production daemon.
- Local live-runtime status now separates proven local capability from current warm runtime state through `currentRuntimeState`. It can report that the one-event runtime is proven but loops are stopped, partially running, or warm in no-quota mode, and it returns the next operator action without starting loops or spending provider quota. This makes internal tester readiness clearer while leaving installed unattended service ownership as P1.
