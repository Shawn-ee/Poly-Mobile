# Live Runtime P0/P1/P2 Gap List

| Gap | Priority | Status |
| --- | --- | --- |
| Determine whether existing Odds API path is replay, one-shot, or continuous | P0 | Complete: replay and one-shot existed; continuous proof runner added for this goal. |
| Prove one upcoming event can refresh from live provider data | P0 | Complete: Spain vs. France live provider proof passed. |
| Prove stale-to-ready provider lifecycle transition | P0 | Complete: selected quote lifecycle went stale -> ready. |
| Prove local maker quotes shifted worse than provider | P0 | Complete: latest provider refresh proof for the selected `Over 2.5` outcome reports reference bid/ask `0.4735/0.5135`; the latest no-quota local maker seed after the S23 cashout proof restored quote-route bid/ask visibility at `0.58/0.59` for the selected outcome. |
| Prove fake-token buy/Portfolio/cashout/history against refreshed event | P0 | Complete: backend proof passed and S23 proof passed for Home -> Event Detail -> provider totals line -> ticket -> buy -> Portfolio -> sell/cashout -> History. |
| Anchor cashout ticket quantity/proceeds to server-owned position estimate | P0 | Complete: `GET /api/portfolio/cash-out/estimate` now exposes the existing `estimateCashOut` service to mobile with `account:read`; Portfolio/Event Detail cashout attempts this route before falling back to quote/position fields, so Max can be driven by owned server shares and current sell bid rather than wallet balance. |
| Provide one-command local runtime restart check | P0 | Complete: `npm run mobile:one-event-live-runtime` starts/checks backend, verifies Docker/Postgres, reports S23 reachability, and validates cached one-event live proof without spending provider quota. |
| Ensure managed S23 mobile launch is server-backed | P0 | Complete: `scripts/manage_holiwyn_internal_tester_runtime.ps1` now starts manager-owned Expo with `EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:3002`, `EXPO_PUBLIC_ORDER_MODE=server`, `EXPO_PUBLIC_MARKET_DATA_MODE=server`, and order book hidden. When the S23 is connected, startup configures ADB reverse for backend and Expo ports and fails readiness if that port forwarding fails. |
| Gate managed S23 server-backed startup in phase/completion audits | P0 | Complete: `npm run mobile:one-event-phase-audit` now includes `managed-s23-server-mode-startup`, and `npm run mobile:live-runtime-completion-audit` now includes `managedS23ServerModeStartupKnown` inside the runtime-launch requirement. The gate also requires the external-Expo unverified warning contract so reused stale Expo listeners cannot be mistaken for verified S23 server-mode proof. |
| Provide reusable local maker liquidity seed for internal testers | P0 | Complete: `npm run mobile:one-event-live-runtime -- -SeedMaker` seeds resting shifted bid/ask liquidity for the selected provider-backed market. Latest committed evidence selects `Over 2.5`, places/restores local maker liquidity, and the quote route reports visible bid/ask liquidity at `0.58/0.59`. |
| Gate current selected-outcome quote liquidity | P0 | Complete: `npm run mobile:one-event-runtime-status`, `npm run mobile:one-event-phase-audit`, `npm run mobile:live-runtime-completion-audit`, and `GET /api/internal/live-runtime/status` now require the active selected outcome to appear in `GET /api/markets/:marketId/quote` and fail if either bid or ask is missing. Cycle ZZC first caught missing selected-outcome ask liquidity, then `npm run mobile:one-event-live-maker-seed` restored a visible ask for Spain vs. France `Over 2.5`; Cycle ZZD aligned completion/status surfaces to the same outcome; Cycle ZAC now scores selected-market candidates by quote-route visibility, so stale provider proof outcome ids such as `Over +2.5` cannot outrank currently tradable `Over 2.5` maker evidence. |
| Refresh selected-outcome runtime identity after S23 cashout proof | P0 | Complete: Cycle ZBR reran `npm run mobile:one-event-live-maker-seed` after the S23 buy/cashout proof consumed ask-side liquidity. The refreshed maker seed selected current outcome id `caa93a28-258a-4bc7-8dc0-10afe9f342b8`, showed quote-route bid/ask `0.58/0.59`, and the ordered live-runtime audit gate passed with zero P0 gaps. |
| Keep quote-route labels mobile-facing | P0 | Complete: `GET /api/markets/:marketId/quote` now returns clean `outcomeName` values such as `Over 2.5` for totals/team-total markets while preserving raw provider labels such as `Over +2.5` in `referenceOutcomeLabel`. Runtime status proof confirms the selected Spain vs. France quote route uses the clean label. |
| Prove event lifecycle controls for selected live event | P0 | Complete: `npm run mobile:one-event-lifecycle-proof` proves `LIVE` accepts orders, `PAUSED` and `CLOSED` reject with `MARKET_UNAVAILABLE`, settlement preview is non-mutating, and original market status is restored. |
| Prove local start-time lifecycle scheduler | P0 | Complete: `npm run mobile:one-event-lifecycle-scheduler-proof` proves no action before suspend window, `PAUSED` inside suspend window, `CLOSED` at/after start, paused/closed order rejection, event/market restore, and maker reseed after proof. |
| Provide one-event lifecycle matrix | P0 | Complete: `npm run mobile:one-event-lifecycle-matrix` summarizes open, paused, closed, and settled/resolved evidence in one read-only report, confirms no active tester settlement execution, and records current P1 limitations. |
| Provide consolidated one-event readiness gate | P0 | Complete: `npm run mobile:one-event-live-readiness` runs data hygiene, runtime, maker seed, manual lifecycle controls, start-time lifecycle scheduler, backend health, Docker/Postgres, S23 reachability, cached live provider proof, and S23 visible proof checks into one summary. |
| Provide one-command local event onboarding | P0 | Complete: `npm run mobile:one-event-onboarding` replays/imports the selected provider-shaped event without quota, runs readiness, runtime status, settlement readiness, and settlement dry-run, and writes `one-event-onboarding-summary.redacted.json`. The optional backend-runtime proof mode `npm run mobile:one-event-onboarding -- -AllowDisconnectedS23 -StartRuntimeLoops -StopRuntimeLoopsAfterProof` starts the local supervisor/result-poller, proves both are running, then stops them and writes dedicated start/status/stop summaries. |
| Provide repeated local one-event supervisor | P0 | Complete: `npm run mobile:one-event-live-supervisor -- -MaxIterations 2 -IntervalSeconds 1 -SkipSleep` ran two local cycles, spent no provider quota, ran data hygiene each cycle, reseeded shifted maker bid/ask quotes, and ran the safe real-time lifecycle scheduler each cycle. |
| Make continuous supervisor proof process-safe | P0 | Complete: `npm run mobile:one-event-live-supervisor:continuous-proof` now starts proof-owned backend processes directly, waits for health, runs the repeated supervisor proof with skip-sleep settings, and stops proof-owned backend process trees after completion. |
| Add quota cap/cadence to live provider supervisor | P0 | Complete: `-RunProviderProof` is now capped by `-MaxProviderProofRuns` and paced by `-ProviderProofEveryIterations`; supervisor summaries report completed provider proof runs, cadence, max run cap, and max possible credits across provider proofs. |
| Provide no-quota runtime status report | P0 | Complete: `npm run mobile:one-event-runtime-status` reports cached-vs-live provider mode, last live proof freshness, last quota usage, local quote route health, maker seed status, supervisor truth, and scheduler state without calling The Odds API. |
| Keep live-runtime S23 proof evidence fresh | P0 | Complete: phase/completion audit scripts now discover the newest passing Spain vs. France S23 cashout proof with close-position, owned-share Max, and no Yes/No selector assertions instead of hardcoding an older proof path. Latest full-flow S23 evidence for this path is `cycle-ZBR-odds-api-live-refresh-s23-flow`; latest focused phone-side Max evidence is `docs/mobile/audits/cycle-S23CASHOUTMAX-close-position-max.md`. |
| Keep consolidated readiness aligned with fresh S23 proof | P0 | Complete: `npm run mobile:one-event-live-readiness` now loads the local database environment for child commands and discovers the newest passing Spain vs. France S23 cashout proof instead of hardcoding older proof folders. Latest committed completion evidence points to `cycle-ZBR-odds-api-live-refresh-s23-flow`. |
| Separate latest-run profile from proven runtime capabilities | P0 | Complete: `npm run mobile:one-event-runtime-status` now reports the latest supervisor run profile separately from proven continuous supervisor/result-poller capabilities, so a narrow latest proof does not hide earlier passing repeated-runtime evidence. |
| Gate runtime capability truth in phase audit | P0 | Complete: `npm run mobile:one-event-phase-audit` now fails unless runtime status preserves proven continuous supervisor and result-poller capability evidence separately from the latest supervisor run profile. |
| Local background supervisor process manager | P0 | Complete: `npm run mobile:one-event-live-supervisor:process -- -Action start -MaxIterations 2 -IntervalSeconds 1 -SkipSleep -WaitForCompletion` proved hidden background supervisor start, completion, summary writing, and no leftover process. |
| Prove one-event settlement readiness | P0 | Complete: `npm run mobile:one-event-settlement-readiness` previews both selected outcomes without mutation, payout conservation passes, the market remains unresolved, and the missing official result provider is explicit. |
| Prove settlement execution safely | P0 | Complete: `npm run mobile:one-event-settlement-execution-proof` executes real orderbook settlement on a deterministic disposable local market, proving payout conservation, collateral zero after settlement, finalized positions, no negative balances, and no stuck locks without mutating the active tester event. |
| Provide guarded one-event manual settlement command | P0 | Complete: `npm run mobile:one-event-settlement -- --winningOutcome=over` dry-runs the selected event settlement, proves payout conservation, prints the exact execution confirmation phrase, and leaves the market unresolved by default. |
| Provide trusted-result settlement intake | P0 | Complete: `npm run mobile:one-event-result-settlement` reads trusted result JSON, maps the final score to the selected market's winning outcome, previews settlement without mutation, and prints a result-digest confirmation phrase for guarded execution. |
| Provide active one-event settlement preflight | P0 | Complete: `npm run mobile:one-event-settlement-preflight` dry-runs trusted-result settlement for the active tester event, reports final result evidence, payout conservation, exact confirmation phrase, current market status, and execution blockers without provider quota or mutation. Latest blocker is `market_not_closed_for_execution:LIVE`. |
| Provide durable settlement audit evidence | P0 | Complete: `npm run mobile:one-event-settlement-audit-event-proof` runs trusted-result settlement dry-run with explicit audit logging, verifies a canonical `settlement.trusted_result.preflight` market event exists for the selected market/outcome/result digest, and confirms no provider quota or active-market settlement mutation. |
| Provide durable settlement approval audit evidence | P1 | Narrowed: `npm run mobile:one-event-settlement-approval-audit-event-proof` derives the active event's approval from trusted-result dry-run output, writes a canonical `settlement.trusted_result.approved` event, exports the matching local approval file for scheduler compatibility, and confirms the active market remains `LIVE` and unresolved. |
| Provide durable result-ingestion audit evidence | P0 | Complete: `npm run mobile:one-event-result-ingestion-audit-event-proof` runs provider-shaped result ingestion with explicit audit logging, verifies a canonical `provider.result.ingested` event exists for the trusted result digest, and confirms no provider quota or settlement execution. |
| Provide one-event result review trail | P0 | Complete: `npm run mobile:one-event-result-review-trail` reads canonical provider-result, settlement-preflight, and settlement-approval events for the active event, reports the selected market/outcome, verifies the approval digest matches the preflight digest, confirms no provider quota or active-market settlement execution, and tells the operator to wait for `CLOSED` status before execution. |
| Provide local result-review backend route | P0 | Complete: `GET /api/internal/live-runtime/result-review` exposes the same canonical provider-result, settlement-preflight, settlement-approval, and settlement-execution audit trail through a dev-only read-only backend route, redacts exact confirmation strings, spends no provider quota, and is now gated by `npm run mobile:one-event-phase-audit`. |
| Prevent repeat settlement execution after execution evidence exists | P0 | Complete: `GET /api/internal/live-runtime/result-review` now treats canonical `settlement.trusted_result.executed` evidence as terminal, persists `executionDecision="already_executed"` and `executionEligibleNow=false`, and reports `repeatExecutionBlocked=true` without exposing exact confirmation text or spending provider quota. |
| Expose blocked settlement attempt evidence | P1 | Narrowed: `GET /api/internal/live-runtime/result-review` and `/api/internal/live-runtime/status` now expose redacted canonical `settlement.trusted_result.blocked` evidence, including blocked reason and current market status, while preserving no-quota/no-execution truth. This improves local operator diagnosis, but authenticated operator UI and production execution controls remain open. |
| Provide local settlement queue backend route | P0 | Complete: `GET /api/internal/live-runtime/settlement-queue` exposes durable `OfficialResultReview` rows as a redacted local operator queue, reports pending/approved/waiting-for-close counts, safe next actions, and no-execution/no-quota truth, and is gated by `npm run mobile:one-event-phase-audit`. |
| Expose durable settlement approval evidence | P1 | Narrowed: settlement queue/status now expose redacted `approvalEvidence` derived from durable `OfficialResultReview` rows and canonical approval event IDs. Phase and completion audits fail if an approved review lacks canonical approval evidence. This narrows the first-class approval evidence gap, but authenticated operator UI and production execution controls remain open. |
| Expose durable settlement execution evidence | P1 | Narrowed: settlement queue/status now expose redacted `executionEvidence` derived from durable `OfficialResultReview` rows and canonical execution event IDs. Phase audit fails if executed reviews lack canonical execution evidence, while unexecuted active tester rows remain explicitly `not_executed` and exact confirmation strings stay redacted. Authenticated operator UI and production execution controls remain open. |
| Gate completion/status on result-review evidence | P0 | Complete: `npm run mobile:live-runtime-completion-audit` now requires phase-gated result-review route evidence, and `GET /api/internal/live-runtime/status` surfaces `resultReview` readiness. Missing canonical review evidence now makes local runtime status need attention. |
| Gate runtime status on settlement queue evidence | P0 | Complete: `GET /api/internal/live-runtime/status` now surfaces `settlementQueue` readiness, queue counts, first pending item identity, and the redacted operator next command. Runtime status and phase audit now fail if settlement queue evidence is missing, unsafe, quota-spending, execution-attempting, exposing exact confirmation strings, or missing the redacted operator plan. |
| Provide first-class durable result-review record | P1 | Narrowed: `OfficialResultReview` now stores a redacted one-event provider-result/preflight/approval review row from `/api/internal/live-runtime/result-review`, and phase/completion/status gates require it. It does not store exact confirmation text, does not spend provider quota, and does not execute settlement. |
| Provide active-event settlement execution decision | P0 | Complete: `npm run mobile:one-event-active-settlement-readiness` reads the active market, trusted-result preflight, approval audit event, result review trail, supervisor wait proof, and active-event clone proof, then reports the exact current execution decision without provider quota or mutation. Current decision: wait for/apply market close before execution because the selected active market is still `LIVE`. |
| Prove trusted-result scheduler execution safely | P0 | Complete: `npm run mobile:one-event-result-settlement-execution-proof` creates a disposable sportsbook-shaped event, runs trusted-result scheduler dry-run, proves execution is blocked while the market is still `LIVE`, closes the disposable market, executes with the exact confirmation phrase, resolves the disposable market, clears collateral/open positions, and confirms the active tester event is not mutated. |
| Keep trusted-result scheduler proof valid while tester runtime is warm | P0 | Complete: Cycle ZBC refreshed the trusted-result scheduler execution proof while the local supervisor/result-poller were running. The proof now compares active-event settlement state only, so ordinary live-runtime collateral/order changes do not create false mutation failures, while any active status/resolution/settlement change still fails the gate. `GET /api/internal/live-runtime/lifecycle`, `npm run mobile:one-event-runtime-status`, `npm run mobile:one-event-phase-audit`, and `npm run mobile:live-runtime-completion-audit` all accept the refreshed proof and report zero open P0 gaps. |
| Prove approved trusted-result auto-settlement safely | P0 | Complete: `npm run mobile:one-event-approved-auto-settlement-proof` creates a disposable sportsbook-shaped event, writes an exact local approval file, proves approved auto-execution waits while `LIVE`, closes the disposable market, auto-executes after approval, resolves the market, and writes a canonical executed audit event without provider quota. |
| Prove active-event market-shape settlement safely | P1 | Narrowed: `npm run mobile:one-event-active-settlement-clone-proof` clones the active Spain vs. France selected Total Goals 2.5 market into a disposable event, runs approved trusted-result auto-execution after close, proves the clone resolves and writes an executed audit event, and confirms the active tester event remains `LIVE` and unresolved. |
| Prove active-event closed-state settlement eligibility | P1 | Narrowed: `npm run mobile:one-event-active-settlement-closed-eligibility-proof` temporarily closes the selected active market, proves trusted-result dry-run eligibility with exact confirmation evidence, confirms no settlement execution, restores the active market to its original `LIVE`/unresolved state, and spends no provider quota. |
| Wire approved settlement wait mode into local supervisor | P0 | Complete: `npm run mobile:one-event-supervisor-approved-settlement-proof` writes an exact active-event approval file from trusted-result dry run evidence, runs one supervisor cycle with approved settlement mode, verifies approval matching, and confirms the active `LIVE` market waits instead of executing. |
| Provide provider-shaped result ingestion | P0 | Complete: `npm run mobile:one-event-result-ingest` converts a redacted Odds API scores-shaped payload into trusted result JSON without spending quota. Live scores ingestion is available only with explicit `--live` and `THE_ODDS_API_KEY`. |
| Prove stale provider trading guard | P0 | Complete: `npm run mobile:one-event-stale-guard-proof` forces selected provider snapshots stale, pauses the `LIVE` market with `paused_provider_stale`, verifies order placement rejects with `MARKET_UNAVAILABLE`, and restores the market/snapshots afterward. |
| Prevent stale replay from overwriting current one-event runtime | P0 | Complete: `npm run mobile:one-event-onboarding` now blocks past replay fixtures by default, restores the cached live-runtime event when applicable, and requires `-AllowPastReplay` for intentional historical replay. |
| Add stale-provider guard to local supervisor loop | P0 | Complete: `npm run mobile:one-event-live-supervisor -- -RunStaleGuard -MaxIterations 1 -IntervalSeconds 0 -SkipSleep` runs the guard in dry-run monitor mode and reports would-pause counts without mutating tester markets. Enforcement is available through `-EnforceStaleGuard`. |
| Add result ingestion to local supervisor loop | P0 | Complete: `npm run mobile:one-event-live-supervisor -- -RunResultIngestion -RunResultSettlement` regenerates trusted result JSON from provider-shaped replay evidence and then dry-runs trusted-result settlement scheduling while the supervisor runs. |
| Add supervisor result-ingestion audit gate | P0 | Complete: `npm run mobile:one-event-phase-audit` now fails if the repeated supervisor proof does not show result ingestion and result-settlement scheduling running while the supervisor loop is active. |
| Provide dedicated local result polling runner | P0 | Complete: `npm run mobile:one-event-result-poller-proof` runs two local polling cycles, regenerates trusted result JSON from provider-shaped score evidence, runs trusted-result settlement scheduling each cycle, spends no provider quota by default, and does not execute active tester settlement. |
| Provide background result poller process manager | P0 | Complete: `npm run mobile:one-event-result-poller:continuous-proof` starts the dedicated result poller through `npm run mobile:one-event-result-poller:process`, verifies heartbeat progress while the process is running, stops the process tree cleanly, spends no provider quota, and does not execute active tester settlement. |
| Provide local internal tester runtime manager | P0 | Complete: `npm run mobile:internal-tester-runtime -- -Action status` reports backend health, Docker/Postgres health, Expo port state, S23 reachability, and supervisor process state without spending provider quota. Start/stop controls exist for manager-owned backend/Expo processes. |
| Report reused Expo server-mode uncertainty | P0 | Complete: the internal tester runtime manager now reports verified server-mode env only when it owns the Expo process. If it reuses an existing Expo listener, the summary exposes `externalExpoServerModeUnverified=true`, `expo.serverModeSource=external_listener_unverified`, and a P1 operator warning to restart with `-Force -ReplaceExternalExpo` or stop stale Expo before trusting S23 server-mode behavior. |
| Provide explicit verified Expo replacement path | P0 | Complete: `scripts/manage_holiwyn_internal_tester_runtime.ps1` now supports `-Force -ReplaceExternalExpo` to intentionally replace an external Expo/Metro listener on the configured Expo port with a manager-owned server-mode Expo process. Plain `-Force` remains non-destructive for external Expo and reports the listener as unverified. Phase/completion audits require this replacement contract. |
| Project verified Expo replacement command in local status | P0 | Complete: `npm run mobile:local-runtime-launch-profile` and `/api/internal/live-runtime/status` now expose the verified Expo replacement command in the manual foreground command list. Phase/completion audits require the launch profile to document this command. |
| Make phase audit runnable from a clean local worktree | P0 | Complete: `npm run mobile:one-event-phase-audit` now loads required local env values from `DOTENV_CONFIG_PATH`, the current worktree `.env`, or nearby project `.env` candidates before resolving the local audit admin user. This keeps the authenticated result-review/settlement-queue audit gate repeatable without committing secrets. |
| Make S23 manual test credential setup warning-tolerant | P0 | Complete: `npm run mobile:manual-testing-env` now captures harmless local configuration warnings from the mobile dev credential command, preserves the real exit-code failure gate, writes the local-only server-mode env file under `.runtime`, and does not print or commit the generated API key. This keeps S23 internal tester startup repeatable when optional deposit/admin environment values are intentionally absent. |
| Add result poller to local internal tester runtime manager | P0 | Complete: `npm run mobile:internal-tester-result-poller-control` proves the internal tester runtime manager can start, report, and stop the dedicated result poller while heartbeat progress advances, without provider quota or active-event settlement execution. |
| Provide local scheduled-task install plan | P1 | Narrowed: `npm run mobile:local-runtime-task -- -Action plan -StartSupervisor -StartResultPoller -RunResultIngestion -RunResultSettlement -RunApprovedResultSettlement` produces a Windows AtLogon scheduled-task plan for the internal tester runtime, carries supervisor/result-poller/approved-settlement wait controls, spends no provider quota, and does not mutate the OS unless rerun with `-Apply`. |
| Audit local scheduled-task install/uninstall permission | P1 | Narrowed: `npm run mobile:local-runtime-task:install-proof` safely attempted install/remove, confirmed Windows denies task registration in the current process context, verified cleanup leaves no task installed, spent no provider quota, and proved the task profile includes the result poller, active approval path, and approved-settlement wait mode. |
| Provide user Startup launcher fallback | P1 | Narrowed: `npm run mobile:local-runtime-startup -- -Action plan -StartSupervisor -StartResultPoller -RunResultIngestion -RunResultSettlement` previews a current-user Windows Startup launcher, and `npm run mobile:local-runtime-startup:install-proof` installs/removes a proof-only launcher while leaving no launcher behind. This is a local user-logon fallback, not a production service. |
| Prove Startup launcher can carry approved-settlement supervisor and result-poller profile | P0 | Complete: `npm run mobile:local-runtime-startup:install-proof` now installs/removes a proof-only launcher whose generated command starts backend/Expo/supervisor and the dedicated result poller with result ingestion, trusted-result settlement scheduling, and approved-settlement wait mode. It spends no provider quota and leaves no persistent launcher installed. |
| Prove scheduled-task can carry approved-settlement supervisor and result-poller profile | P0 | Complete: `npm run mobile:local-runtime-task:install-proof` now verifies the generated scheduled-task plan starts backend/Expo/supervisor and the dedicated result poller with result ingestion, trusted-result settlement scheduling, the active approval file, and approved-settlement wait mode. It spends no provider quota and leaves no persistent task installed; actual task registration remains P1 blocked by Windows permissions. |
| Provide local runtime launch profile | P0 | Complete: `npm run mobile:local-runtime-launch-profile` consolidates manual foreground, user Startup fallback, scheduled-task blocker, and live-provider opt-in commands into one no-quota operator report, and `npm run mobile:one-event-phase-audit` now gates on it. |
| Provide live-runtime completion truth audit | P0 | Complete: `npm run mobile:live-runtime-completion-audit` answers the phase completion questions in one no-quota artifact: maker continuity, live/replay odds mode, refresh cadence, quota protection, stale handling, lifecycle state, S23 trade proof, and remaining P1 production gaps. |
| Provide ordered live-runtime audit gate | P0 | Complete: `npm run mobile:live-runtime-audit-gate` runs runtime status, phase audit, and completion audit in the correct order, writes one no-quota redacted summary, and prevents completion evidence from reading stale failed phase artifacts. |
| Provide internal tester readiness gate | P0 | Complete: `npm run mobile:internal-tester-readiness-gate` runs the ordered audit gate, refreshes the operator snapshot/checklist, writes one no-quota go/no-go summary, and reports the current event, selected market, recommended command, manual S23 flow, lifecycle checks, and P0/P1/P2 gaps. |
| Separate cached trading readiness from live-odds readiness | P0 | Complete: `npm run mobile:internal-tester-readiness-gate` now reports `cachedTradingReady`, `liveOddsReady`, the cached/no-quota command, and the explicit live-odds refresh command. Cached trading can pass while live-odds readiness stays false if mobile provider snapshots are stale. |
| Provide no-quota live odds refresh preflight | P0 | Complete: `npm run mobile:live-odds-refresh-preflight` reads the latest local readiness/runtime/audit evidence, reports whether the live provider refresh command is known, whether a provider key is configured through the shell or ignored `.runtime/secrets/the-odds-api-key.txt` secret file without printing it, whether quota-spending loops are already running, and whether mobile provider snapshots are stale. The preflight spends no provider quota, starts no loops, mutates no markets, and keeps missing provider key/fresh live odds as P1 unless an operator explicitly chooses the live refresh path. |
| Guard live-refresh commands against concurrent quota loops | P0 | Complete: both `npm run mobile:one-event-live-runtime:provider` and `npm run mobile:one-event-live-runtime:provider-secret` run the no-quota live-odds refresh preflight before spending provider quota, refuse to start if another quota-spending loop is already reported running, and expose an explicit `-ForceProviderRefresh` override for intentional operator use. The secret wrapper runs the guard once, then delegates to the lower-level provider command with duplicate preflight skipped. |
| Make live-runtime completion requirements machine-readable | P0 | Complete: `npm run mobile:live-runtime-completion-audit` now writes `completionRequirements` rows for market-maker continuity, odds refresh mode, quota protection, stale odds handling, event lifecycle, one-event mobile trading, and runtime launch. `/api/internal/live-runtime/status` projects those rows and reports `needs_attention` if they are missing or failed; the phase audit gates on them. |
| Gate completion audit on internal tester watchdog | P0 | Complete: `npm run mobile:live-runtime-completion-audit` now fails if the watchdog proof no longer proves base runtime readiness, supervisor proof, result-poller proof, no-quota default mode, and loop cleanup. |
| Expose local live-runtime status to internal tools | P0 | Complete: `GET /api/internal/live-runtime/status` returns a redacted dev-only status summary backed by completion audit, phase audit, and watchdog proof artifacts, refuses production, and spends no provider quota. |
| Expose local runtime launch/install commands in status | P1 | Narrowed: `GET /api/internal/live-runtime/status` now projects recommended Startup plan/install/uninstall commands, scheduled-task plan/install commands, and explicit live-provider opt-in commands from the launch-profile artifact. It does not install anything or replace production service ownership. |
| Gate audit completion on launch/install command visibility | P1 | Narrowed: `npm run mobile:one-event-phase-audit` and `npm run mobile:live-runtime-completion-audit` now fail if `serviceOwnership.localLaunch` no longer exposes the recommended Startup commands, scheduled-task commands, live-provider opt-in commands, and quota-safety flags. This protects the local operator contract but still does not install an unattended production service. |
| Project runtime ownership proof in local status | P1 | Narrowed: `npm run mobile:local-runtime-launch-profile` now writes `ownershipProof`, and `GET /api/internal/live-runtime/status` projects it under `serviceOwnership.localLaunch`. Phase/completion audits require Startup proof/current install truth, scheduled-task proof/current install truth, Windows permission blocker truth, foreground process proof, no-quota default truth, and active-settlement no-execution truth. This improves local operator visibility but still does not install an unattended production service. |
| Gate local runtime status on proof artifact freshness | P0 | Complete: completion audit now fails when the live provider proof or watchdog proof is stale, and `GET /api/internal/live-runtime/status` returns `needs_attention` unless completion audit, phase audit, and watchdog artifacts are all passing and under the 24-hour local freshness window. |
| Re-age embedded live-provider proof in local status | P0 | Complete: `GET /api/internal/live-runtime/status` now adds completion-audit wall-clock age to the audit's embedded live-provider proof age, so a fresh completion artifact cannot keep a provider proof ready after its own max age expires. |
| Cross-check selected market provider snapshots in DB | P0 | Complete: `GET /api/internal/live-runtime/status` now reads `ReferenceQuoteSnapshot` rows for the selected market, reports source/count/latest fetched age/accepting/mm-eligible counts, and returns `needs_attention` if the selected market has no fresh stored provider snapshot under the live proof max-age window. |
| Gate phase audit on local live-runtime status API | P0 | Complete: `npm run mobile:one-event-phase-audit` now fails unless `GET /api/internal/live-runtime/status` reports ready, zero P0 gaps, fresh proof artifacts, no provider quota usage by status, and fresh selected-market DB provider snapshots. |
| Report current managed loop process state in local runtime status | P0 | Complete: `GET /api/internal/live-runtime/status` now reports one-event supervisor and result-poller process visibility from local `.runtime` state plus OS pid checks, including whether a loop is running and whether any running loop is quota-spending. Phase audit requires this visibility while preserving no-quota default status checks. |
| Separate proof freshness from mobile-route provider freshness | P0 | Complete: `GET /api/internal/live-runtime/status` now reports the selected market's local proof-window freshness separately from 60/90-second mobile-route freshness, including current mobile lifecycle status, refresh/stale timestamps, and next provider action. |
| Provide operator next-action guidance from local runtime status | P0 | Complete: `GET /api/internal/live-runtime/status` now returns local command guidance for cached internal testing, quota-capped provider refresh, cached supervisor startup, and cached result-poller startup, with provider-key and quota flags. |
| Surface active settlement decision in local runtime status | P0 | Complete: `GET /api/internal/live-runtime/status` now reports the active-event settlement decision, blocker list, closed-market guard, active-event closed-state eligibility proof, no-execution truth, disposable clone proof truth, and next safe action without exposing the exact confirmation phrase. |
| Separate latest supervisor profile from proven capabilities in local status | P0 | Complete: `GET /api/internal/live-runtime/status` now returns `runtimeCapabilities`, so a narrow latest supervisor proof does not hide previously proven repeated maker reseed, lifecycle scheduling, result ingestion, result settlement scheduling, and background result-poller capabilities. |
| Keep no-quota cached testing as default status action | P0 | Complete: `GET /api/internal/live-runtime/status` now keeps `operatorNextActions.recommendedFirstAction=cached_internal_testing` when local proof is ready but mobile-display odds are stale, while still exposing `liveOddsAction=refresh_mobile_live_odds` as an explicit quota-spending option. |
| Separate proven capability from current warm runtime state | P0 | Complete: `GET /api/internal/live-runtime/status` now returns `currentRuntimeState`, distinguishing `proven_capability_loops_stopped`, `partial_runtime_running`, `warm_no_quota_runtime`, and `not_ready`. Phase and completion audits require this current-state decision with zero P0 gaps, while preserving the truth that stopped local loops are not the same as a warm tester runtime. |
| Start warm no-quota internal tester loops from manager | P0 | Complete: local runtime manager now defaults approved-settlement startup to `trusted-result-audit-approved.redacted.json`, the active-event approval proof, and the supervisor process wrapper no longer blocks the manager while launching a continuous loop. Full manager proof passed with backend 3002 ready, Expo 8081 ready, Docker/Postgres healthy, supervisor running, result poller running, and `quotaSpendingLoopRunning=false`; loops were then stopped for process hygiene. |
| Prove current warm no-quota runtime state end-to-end | P0 | Complete: `npm run mobile:current-runtime-state-proof` now starts the local internal tester runtime, observes `/api/internal/live-runtime/status` with `currentRuntimeState.mode=warm_no_quota_runtime`, supervisor and result poller running, no quota-spending loop, and then stops all manager-owned processes. Phase and completion audits require `current-runtime-state-proof-summary.redacted.json` before passing. |
| Connect warm runtime proof to tester readiness gate | P0 | Complete: `npm run mobile:current-runtime-state-proof` now runs `npm run mobile:internal-tester-readiness-gate` while the local supervisor/result-poller loops are active, records whether the gate sees warm no-quota runtime and all loops running, and still cleans up loops after proof. Cached/no-quota mode may still report stale mobile provider snapshots as P1. |
| Provide authoritative live-runtime phase audit | P0 | Complete: `npm run mobile:one-event-phase-audit` reads current proof artifacts plus local health/quote routes and reports 0 open P0 gaps for the one-event local internal runtime, with P1 unattended service and official-result auto-settlement still open. |
| Provide internal tester runtime watchdog proof | P0 | Complete: `npm run mobile:internal-tester-watchdog-proof` verifies backend, Expo, and Docker/Postgres readiness, runs the repeated local supervisor proof, runs the background result-poller proof, stops loop processes after the proof, spends no provider quota by default, and writes `internal-tester-watchdog-summary.redacted.json`. |
| Gate internal tester watchdog in phase audit | P0 | Complete: `npm run mobile:one-event-phase-audit` now fails if the watchdog proof is missing or no longer proves backend/Expo/Postgres readiness, repeated supervisor proof, background result-poller proof, no-quota default mode, and loop cleanup. |
| Provide local lifecycle backend route | P0 | Complete: `GET /api/internal/live-runtime/lifecycle` exposes open, suspended, closed, and settled/resolved lifecycle proof through a dev-only read-only backend route, spends no provider quota, does not mutate markets, confirms active tester settlement has not executed, and is gated by `npm run mobile:one-event-phase-audit`. |
| Durable provider refresh run evidence | P1 | Narrowed: `ProviderRefreshRun` now stores the selected one-event bounded live-provider refresh proof outcome. `GET /api/internal/live-runtime/status`, phase audit, and completion audit require the latest selected-event row to be passed, quota-protected, within budget, stale-before-refresh, and ready-after-refresh. This closes proof-artifact-only provider refresh outcome evidence, but not installed multi-event provider polling. |
| Durable market-maker quote run evidence | P1 | Narrowed: `MarketMakerQuoteRun` now stores the selected market's shifted local maker quote proof outcome. `GET /api/internal/live-runtime/status`, phase audit, and completion audit require the latest selected-market row to be passed, local-only, shifted worse than provider, quote-route visible, and backed by a fresh provider snapshot. This closes proof-artifact-only maker quote evidence, but not installed continuous market-maker service ownership. |
| Repeated local maker quote run evidence | P1 | Narrowed: `GET /api/internal/live-runtime/status` now reports recent `MarketMakerQuoteRun` rows and requires at least two passed local rows emitted by `scripts/seed_odds_api_live_shifted_maker.ts`. Phase and completion audits now fail unless repeated local maker quote runs are proven. This strengthens local maker continuity evidence, but still does not install a continuous production market-maker service. |
| Provider refresh to maker quote handoff | P1 | Narrowed: `npm run mobile:provider-maker-handoff` proves the selected one-event `ProviderRefreshRun` is followed by a later shifted local `MarketMakerQuoteRun` for the same event, market, and outcome. Phase and completion audits now require this no-quota handoff evidence. This is still not an installed multi-event maker daemon. |
| Worker-owned runtime heartbeat emission | P1 | Narrowed: the one-event supervisor and result poller now call `npm run mobile:runtime-heartbeat` while they run, writing worker-owned `RuntimeServiceHeartbeat` metadata. Phase/completion audits require both service rows to preserve `workerOwned=true`, no quota-spending loop, and no installed-service claim. This improves durable local service evidence but is not an installed production service. |
| Worker-owned runtime run records | P1 | Narrowed: the one-event supervisor and result poller now emit `RuntimeServiceRun` rows after a run finishes. The local status route plus phase/completion audits require latest passed rows for both services, no provider quota spend, no active settlement execution, no installed-service claim, and worker-owned metadata. This closes the proof-artifact-only outcome gap for local workers, but is still not an installed production daemon. |
| Continuous unattended daemon | P1 | Narrowed: `npm run mobile:one-event-live-supervisor:continuous-proof` proves the local background supervisor can start in continuous mode, write a heartbeat after a live cycle, keep running during status/runtime checks, and stop cleanly. `npm run mobile:one-event-onboarding -- -AllowDisconnectedS23 -StartRuntimeLoops -StopRuntimeLoopsAfterProof` now proves the same local supervisor/result-poller startup path through the one-command onboarding wrapper and cleans both loops afterward. `GET /api/internal/live-runtime/status` preserves worker-owned supervisor/result-poller `RuntimeServiceHeartbeat` metadata and latest `RuntimeServiceRun` outcomes while also mirroring current process state, and now exposes `start_full_internal_tester_runtime` as the no-quota operator action for starting/reusing backend/Expo plus cached supervisor/result-poller loops through the internal tester runtime manager. `npm run mobile:internal-tester-runtime -- -Action status/start/stop` provides a local tester control plane for backend, Expo, supervisor, and the dedicated result poller; first-class aliases now expose `npm run mobile:internal-tester-runtime:cached-start`, `npm run mobile:internal-tester-runtime:live-provider-start`, and `npm run mobile:internal-tester-runtime:stop`. The live-provider alias is explicit and key/quota gated. `npm run mobile:local-runtime-task -- -Action plan ...` provides a dry-run Windows scheduled-task install plan with result-poller and approved-settlement wait controls, and `npm run mobile:local-runtime-startup -- -Action plan ...` provides a user Startup launcher fallback. `npm run mobile:local-runtime-task:install-proof` shows this Windows process cannot register the task without elevated/task-registration rights, and no task is left installed. Both scheduled-task and Startup proofs now verify the approved-settlement supervisor plus dedicated result-poller profile without provider quota. |
| Always-on auto-close/suspend daemon | P1 | Narrowed: lifecycle scheduler runs inside the continuous supervisor while it is active and is covered by file heartbeat plus worker-owned durable `RuntimeServiceHeartbeat` and `RuntimeServiceRun` evidence. It is still not installed as an OS service. |
| Automatic official-result settlement | P1 | Narrowed: manual/admin preview/resolve, guarded one-event settlement, provider-shaped score ingestion into trusted-result JSON, durable result-ingestion audit events, active-event settlement preflight, one-event result review trail with approval evidence, dev-only local result-review API, dev-only local settlement queue API with redacted per-item `operatorAction`, first-class redacted `OfficialResultReview` row, active-event settlement execution decision reporting, dedicated local result polling runner, a managed background result poller process, opt-in quota-capped live result ingestion in the poller/supervisor, local trusted-result scheduler dry-run, durable settlement preflight/approval audit events, trusted-result scheduler execution on a disposable sportsbook-shaped event, approval-file auto-execution on a disposable sportsbook-shaped event, active-event market-shape clone execution, active-event closed-state eligibility proof, and supervisor approved-settlement wait mode exist. Trusted-result execution waits until the selected active market is `CLOSED`; current active-event blocker is `market_not_closed_for_execution:LIVE`. Installed unattended live result polling, authenticated operator UI, and direct active-event resolve execution are still missing. |
| Project settlement automation truth in local status | P1 | Narrowed: `GET /api/internal/live-runtime/status` now exposes `settlementAutomation`, a compact status contract for replay/live result ingestion mode, approved scheduler proof, active-event close blocker, approval truth, closed-state eligibility, exact-confirmation redaction, and no-quota/no-execution safety. Phase/completion audits require the block. This improves internal automation visibility but still does not install official-result polling or authenticated production execution controls. |
| Project local-vs-production readiness boundary in local status | P1 | Narrowed: `GET /api/internal/live-runtime/status` now exposes `productionReadinessBoundary`, explicitly reporting local internal runtime readiness separately from `productionReady=false`, fake-token-only truth, no-real-money deployment truth, provider quota safety, official-result automation guards, production blockers, and required production work. Phase/completion audits require this boundary so local proof cannot be mistaken for production automation completion. |
| Structured settlement operator execution plan | P1 | Narrowed: `GET /api/internal/live-runtime/settlement-queue` and `/api/internal/live-runtime/status` now expose a versioned `operatorExecutionPlan` with prerequisites, blocker keys, executable-now truth, redacted command metadata, no-quota truth, no active-execution truth, and exact-confirmation redaction. Phase and completion audits require the structured plan. This improves local operator safety but is still not an authenticated production execution workflow. |
| Project operator controls boundary in local status | P1 | Narrowed: `GET /api/internal/live-runtime/status` now exposes `operatorControlBoundary`, explicitly separating local dev-only read-only result-review/settlement-queue controls from missing authenticated production operator controls. Phase and completion audits require it to prove no public mobile route, no provider quota, no active execution, no exact confirmation exposure/storage, and clear production blockers. This is still not an authenticated operator UI or production execution workflow. |
| Define production operator auth requirements in local status | P1 | Narrowed: `GET /api/internal/live-runtime/status` now exposes `operatorControlBoundary.productionAuthRequirements`, a machine-readable route/schema/guard contract for future authenticated operator session, settlement approval, settlement execution, durable operator identity, role checks, audit events, no public mobile exposure, CLOSED-market guard, and exact-confirmation redaction. Phase and completion audits require this block. The actual authenticated routes/UI/schema remain future P1 work. |
| Provide authenticated operator session route | P1 | Narrowed: `GET /api/internal/operator/session` now returns the authenticated admin operator identity, role list, and review/approval capabilities without provider quota, state mutation, public mobile exposure, execution, or exact-confirmation text. Runtime status and audits mark `operator_session` as `implemented_read_only`. Settlement execution endpoint, dedicated operator roles, two-person/admin approval, durable operator audit events, and production UI remain P1. |
| Protect local result-review and settlement-queue routes with operator auth | P1 | Narrowed: `GET /api/internal/live-runtime/result-review` and `GET /api/internal/live-runtime/settlement-queue` now require `requireAdmin()`, return 401 without auth, and remain no-quota/read-only/redacted with authenticated local admin proof. Dedicated settlement-operator roles and production operator UI remain P1. |
| Provide authenticated settlement approval route | P1 | Narrowed: `POST /api/internal/live-runtime/settlement-queue/:reviewId/approve` now records authenticated operator approval evidence on an existing durable review row and canonical market event, without provider quota, exact-confirmation storage/exposure, active settlement execution, public mobile exposure, or market mutation. Runtime status and audits mark `settlement_approval` as `implemented_guarded_no_execution`. Direct exact-confirmation execution, dedicated production role model, installed official-result polling, and production UI remain P1; Cycle XR adds the dry-run two-person/admin policy check. |
| Provide guarded settlement execution boundary | P1 | Narrowed: `POST /api/internal/live-runtime/settlement-queue/:reviewId/execute` now exists as an authenticated dry-run request route and local exact-confirmed execution route for reviewed CLOSED markets. It records durable operator identity only for guard-passing requests, enforces admin-override-or-different-operator policy, and returns blocked safety reasons for active `LIVE` markets without provider quota, exact-confirmation storage/exposure, public mobile exposure, or unsafe mutation. Dedicated production role model, installed official-result polling, and production UI remain P1. |
| Provide durable operator audit table | P1 | Narrowed: `OperatorAuditEvent` is now a dedicated Prisma model/migration. First-time settlement approval writes `settlement_approval_recorded`, guard-passing execution dry-run writes `settlement_execution_dry_run_requested`, and successful local exact-confirmed execution writes `settlement_execution_completed`, each with operator id, review id, role snapshot, request id, canonical event id, and redacted metadata. Dedicated production settlement-operator role model, production operator UI, and installed official-result polling remain P1. |
| Provide exact-confirmed local settlement execution route | P1 | Narrowed: `POST /api/internal/live-runtime/settlement-queue/:reviewId/execute` now supports local exact-confirmed execution for reviewed CLOSED markets. It remains internal/dev-only, admin-authenticated, production-disabled, provider-quota-free, two-person/admin-policy checked, exact-confirmation redacted/not stored, and blocked unless approval/preflight evidence plus the operator-held confirmation match. Installed official-result polling, dedicated production role model, and production operator UI remain P1. |
| One-event data hygiene guard | P0 | Complete: `npm run mobile:one-event-data-hygiene-proof` verifies listed mobile-visible sportsbook/contract markets belong to the current event title; lifecycle scheduler now only touches `isListed=true` markets. |
| Per-provider-event slugging | P2 | Future improvement: replace the reusable `odds-api-single-soccer-test` slug with per-provider-event slugs once multi-event onboarding starts. |
| Multi-event provider polling | P2 | Intentionally out of scope to protect quota. |
